services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - ./backend:/app
      - ./backend/data:/app/data
    ports:
      - 8000:8000
    env_file:
      - ./backend/.env
    container_name: backend

  # db:
  # cache:
